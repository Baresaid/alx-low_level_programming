#include "main.h"

/**
 * main - check the code
 * prints the alphabet, in lowercase, followed by a new line.
 */
void print_alphabet(void)
{

	char j;
	for (j = 'a'; j <= 'z'; j++)
	{
		_putchar(j);
	}
	_putchar('\n');
}
