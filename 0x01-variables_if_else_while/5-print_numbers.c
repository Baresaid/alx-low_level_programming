#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main -main block
 * Description: Get a random it with 5
 * Return: 0
 */

int main(void)

{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
