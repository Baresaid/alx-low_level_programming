#include <stdio.h>
#include <time.h>
/**
 * main -main block
 * Description: Get a random it with 5
 * Return: 0
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
